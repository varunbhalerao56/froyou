name: froyou
description: "Froyou"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.9
  flutter_native_splash: ^2.4.7
  # go_router: ^16.2.4
  path_provider: ^2.1.5
  path: ^1.9.1
  image: ^4.5.4
  typed_isolate: ^6.0.0
  intl: ^0.20.2
  file_picker: ^10.3.7
  image_picker: ^1.2.2
  url_launcher: ^6.3.2
  device_info_plus: ^12.3.0
  bloc: ^9.0.0
  flutter_bloc: ^9.1.1
  flutter_hooks: ^0.21.3+1
  equatable: ^2.0.7
  shared_preferences: ^2.5.3
  objectbox: ^5.3.2
  objectbox_flutter_libs: any
  hooks: ^1.0.1
  # code_assets: ^1.0.0
  # native_toolchain_rust: ^1.0.3
  # ffi: ^2.1.4
  # flutter_rust_bridge: ^2.11.1
  freezed_annotation: ^3.1.0
  uuid: ^4.5.3
  crypto: ^3.0.7
  palette_generator_master: ^1.1.0
  flutter_shaders: ^0.1.3
  progressive_blur: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  freezed: ^3.2.5
  build_runner: ^2.15.1
  flutter_launcher_icons: ^0.14.4
  objectbox_generator: any

flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  fonts:
    # SF Pro Rounded. Only these four weights exist in the bundle — asking for
    # anything heavier than w700 makes Flutter synthesize a fake bold, so the
    # type scale in app_typography.dart stops there.
    - family: SFProRounded
      fonts:
        - asset: fonts/SF-Pro-Rounded-Regular.otf
        - asset: fonts/SF-Pro-Rounded-Medium.otf
          weight: 500
        - asset: fonts/SF-Pro-Rounded-Semibold.otf
          weight: 600
        - asset: fonts/SF-Pro-Rounded-Bold.otf
          weight: 700

objectbox:
  # Writes objectbox-model.json and objectbox.g.dart to lib/generated
  output_dir: generated

# flutter_launcher_icons:
#   ios: true
#   android: true
#   image_path: "assets/images/app_icon.png"
#   image_path_android: "assets/images/app_icon.png"
#   remove_alpha_ios: true
#   background_color_ios: "#FAFAF6"
#   adaptive_icon_background: "assets/images/ic_launcher/ic_launcher_background.png"
#   adaptive_icon_foreground: "assets/images/ic_launcher/ic_launcher_foreground.png"
#   adaptive_icon_foreground_inset: 10
#   adaptive_icon_monochrome: "assets/images/ic_launcher/ic_launcher_monochrome.png"
#   image_path_ios_tinted_grayscale: "assets/images/app_icon_monochrome.png"
#   desaturate_tinted_to_grayscale_ios: true

# flutter_native_splash:
#   color: "#FAFAF6"
#   color_dark: "#0E0F14"
#   image: "assets/images/app_splash_screen.png"
#   branding: "assets/images/branding.png"
#   branding_dark: "assets/images/branding_dark.png"
#   branding_bottom_padding: 24
#   android_12:
#     image: "assets/images/app_splash.png"
#     color: "#FAFAF6"
#     color_dark: "#0E0F14"
#     branding: "assets/images/branding.png"
#     branding_dark: "assets/images/branding_dark.png"
